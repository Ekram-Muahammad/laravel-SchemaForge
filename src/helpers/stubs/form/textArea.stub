<div class="form-group">
    <label for="{{fieldName}}">{{fieldName}}</label>
    <textarea class="form-control" id="{{fieldName}}" name="{{fieldName}}" rows="5">{{ {{fieldValue}} }}</textarea>
</div>
